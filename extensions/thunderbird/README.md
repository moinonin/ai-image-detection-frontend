# Thunderbird Email Verifier

This add-on sends the selected raw message to the VeriForensic provenance API.
It does not call ns-stego directly or store an ns-stego service token.

Load `manifest.json` as a temporary add-on during development, or package the
directory as an XPI for installation.
